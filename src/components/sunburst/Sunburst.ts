import d3 from '../../utils/d3-import';

/**
 * Initialize the view.
 * @param component Sunburst component
 * @param data Hierarchy data loaded from a JSON file
 */
export const initView = (component: any, data: any) => {
  console.log(data);
};
